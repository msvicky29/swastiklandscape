import React from 'react';
import PageTemplate from './PageTemplate';
import { pageData } from '../data/pageData';

const EventPlant = () => {
  return <PageTemplate pageData={pageData.eventPlant} />;
};

export default EventPlant; 