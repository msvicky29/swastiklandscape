import React from 'react';
import PageTemplate from './PageTemplate';
import { pageData } from '../data/pageData';

const VerticalGarden = () => {
  return <PageTemplate pageData={pageData.verticalGarden} />;
};

export default VerticalGarden; 