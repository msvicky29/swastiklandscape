import React from 'react';
import PageTemplate from './PageTemplate';
import { pageData } from '../data/pageData';

const Irrigation = () => {
  return <PageTemplate pageData={pageData.irrigation} />;
};

export default Irrigation; 